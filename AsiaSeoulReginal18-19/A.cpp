#include<bits/stdc++.h>
#define fo(i,a,b) for(int i=(a);i<=(b);++i)
using namespace std;
/*
Modify a max value!
Add s (which can be +1 or -1)
*/
const int maxn = 1e5+10;
int n;
struct node{

}T[maxn*4];
void get(int x,int l,int r,int L,int R){
    
}
void modify(int x,int l,int r,int L,int R){
    
}
pair<int,int> rec[maxn];
int main(){
    scanf("%d",&n);
    fo(i,1,n){
        int ux,uy,vx,vy;
        scanf("%d%d%d%d",&ux,&uy,&vx,&vy);
        res[i] = make_pair(vy,uy)l
    }
    sort(res+1,res+n+1);
    fo(i,1,n){
        
    }
    return 0;
}
